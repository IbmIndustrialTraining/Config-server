spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
         # uri: git@github.com:IbmIndustrialTraining/ConfigRepository.git
          uri: git@github.com:IbmIndustrialTraining/{application}_config.git  
          #search-paths: dev

server:
  port: 8002
